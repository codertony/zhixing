# 知行平台 (ZhiXing) 环境变量模板
# 复制此文件为 .env.local 并修改配置
#
# 本地开发使用 Podman 作为容器运行时
# 数据库服务通过 Podman 容器启动，不使用 Docker
#

# =============================================================================
# 数据库配置 (PostgreSQL - Podman 容器)
# =============================================================================
# 本地开发使用 Podman 启动的 PostgreSQL 容器
# 容器名称: zhixing-postgres
# 启动命令: podman-compose -f scripts/podman-compose.yml up -d
DB_URL=postgresql://postgres:postgres@localhost:5432/zhixing
DB_POOL_SIZE=10

# =============================================================================
# Redis 缓存 (Podman 容器)
# =============================================================================
# 本地开发使用 Podman 启动的 Redis 容器
# 容器名称: zhixing-redis
REDIS_URL=redis://localhost:6379

# =============================================================================
# Qdrant 向量数据库 (Podman 容器)
# =============================================================================
# 本地开发使用 Podman 启动的 Qdrant 容器
# 容器名称: zhixing-qdrant
QDRANT_URL=http://localhost:6333

# =============================================================================
# API 服务配置
# =============================================================================
API_PORT=3001
API_HOST=0.0.0.0
API_CORS_ORIGIN=http://localhost:5173

# =============================================================================
# Web 客户端配置
# =============================================================================
VITE_API_URL=http://localhost:3001

# =============================================================================
# AI/ML 服务配置
# =============================================================================
# LiteLLM 代理服务地址
AI_LITELLM_URL=http://localhost:4000

# OpenAI API 配置
AI_OPENAI_API_KEY=sk-...
AI_OPENAI_MODEL=gpt-4

# =============================================================================
# GitLab 集成配置
# =============================================================================
GITLAB_URL=https://gitlab.com
GITLAB_TOKEN=glpat-...

# =============================================================================
# 安全配置
# =============================================================================
# JWT 密钥 (生产环境请使用强密码)
JWT_SECRET=your-secret-key-here

# Session 密钥
SESSION_SECRET=your-session-secret-here

# =============================================================================
# MCP Server 配置
# =============================================================================
MCP_SERVER_PORT=3002

# =============================================================================
# 合规追踪配置
# =============================================================================
COMPLIANCE_WEBHOOK_URL=https://hooks.slack.com/...

# =============================================================================
# 开发环境配置
# =============================================================================
NODE_ENV=development
LOG_LEVEL=debug

# =============================================================================
# Podman 容器管理 (本地开发)
# =============================================================================
# 以下容器通过 Podman 管理，不使用 Docker:
# - zhixing-postgres (PostgreSQL 16)
# - zhixing-redis (Redis 7)
# - zhixing-qdrant (Qdrant 向量数据库)
#
# 常用命令:
#   podman ps                    # 查看运行中的容器
#   podman logs -f <name>        # 查看容器日志
#   podman-compose -f scripts/podman-compose.yml up -d    # 启动所有服务
#   podman-compose -f scripts/podman-compose.yml down     # 停止所有服务
#
# 安装 Podman:
#   macOS:   brew install podman podman-compose
#   Linux:   sudo apt-get install podman podman-compose
#   Windows: https://podman-desktop.io/
