# /zhixing:arch

四层架构合规检查

## 描述

检查代码是否符合四层架构规范，检测循环依赖和跨层调用违规

## 用法

```
/zhixing:arch          # 运行所有架构检查
/zhixing:arch check    # 检查跨层调用
```

## 四层架构规则

```
L4 交互平面 → L3 控制平面 → L2 认知平面 → L1 数据平面
```

- 只能自上而下调用
- 禁止跨层调用（如 L4 直接调用 L2）
- 禁止反向调用
